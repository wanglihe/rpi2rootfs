
PACKAGES=()

# raspberry pi official bootloader 
PACKAGES+=(raspberrypi-bootloader)

# drivers
PACKAGES+=(firmware-realtek)

# security
PACKAGES+=(ca-certificates)
PACKAGES+=(ssh)

# base config
PACKAGES+=(locales)
PACKAGES+=(wicd-curses)

# hardware tools
PACKAGES+=(kmod)
PACKAGES+=(cpufrequtils)

# useful tools
PACKAGES+=(vim)
PACKAGES+=(less)
PACKAGES+=(mtr-tiny)
PACKAGES+=(dstat)
PACKAGES+=(htop)
PACKAGES+=(aptitude)
PACKAGES+=(dvtm)
PACKAGES+=(dtach)
PACKAGES+=(strace)
PACKAGES+=(ltrace)

# others
